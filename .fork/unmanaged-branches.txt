# Branches on origin that are in NEITHER manifest, and that is deliberate.
#
# The both-manifest check has an inverse: a branch on origin listed nowhere is
# silently never merged into master. This file is the acknowledged set, seeded
# 2026-08-28 from the state at that moment. Anything appearing on origin
# after the seed is REPORTED, so a new branch cannot go unnoticed.
#
# Every branch listed here STAYS. Being unmanaged is not evidence of being dead:
# a closed-but-unmerged PR branch, an abandoned experiment and a live one all
# look identical from here. To adopt one, move it into features.txt or
# pr-branches.txt and delete its line here -- this list should only ever shrink.
#
# The one other way a line leaves: the branch is provably merged and is deleted
# under "Branch retention" in FORK_RULES.md -- proven, archived to
# refs/fork/archive/<b>, recorded in archived-branches.tsv, owner-approved.
# Nothing here is ever renamed, folded or consolidated.
#
#
#
# 2026-08-28: 56 entries removed here because their branches were deleted --
# provably merged, archived to refs/fork/archive/<b>, recorded in
# archived-branches.tsv, owner-approved. This file lists branches that EXIST
# on origin and are deliberately unmanaged; a line for a branch that is gone
# would be a claim about a ref nobody can look at.
#
# develop, master and feat/fork-tooling are excluded by the check itself.

backup/develop-pre-upstream-sync-20260823
fix/6853-remember-revealed-images
integration/share-sheet-shortcuts
